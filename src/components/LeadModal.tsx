import React from 'react'
import ReactDOM from 'react-dom/client'
import App from './App.tsx'
import './index.css'

  } catch (error) {
        console.log('Calling onReportConfirmed for pending session')
    console.error('Failed to render app:', error)
        console.log('Closing modal for existing flow')
    rootElement.innerHTML = `
      <div style="padding: 20px; font-family: Arial, sans-serif; background: #fee; border: 1px solid #f00;">
        <h1>Render Error</h1>
        <p>Failed to render the React application.</p>
      setError('Failed to submit email. Please try again.')
        <button onclick="window.location.reload()">Reload Page</button>
      </div>
    `
  }
}